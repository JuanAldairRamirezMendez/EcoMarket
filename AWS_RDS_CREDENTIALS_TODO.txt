# INSTRUCCIONES PARA CONFIGURAR AWS RDS
# =====================================

# 1. Ve a AWS Console → RDS → Databases → database-1
# 2. En la sección "Connectivity & security" encontrarás:
#    - Master username: [LO QUE CONFIGURASTE]
#    - Endpoint: database-1.c702yeeu6qxd.us-east-2.rds.amazonaws.com (ya lo tenemos)

# 3. Si no recuerdas la contraseña, puedes:
#    - Ir a "Actions" → "Modify" → "New master password"
#    - O usar la contraseña que configuraste inicialmente

# 4. REEMPLAZA estos valores en el archivo: apps/backend/prisma/.env

# FORMATO:
# DATABASE_URL="postgresql://[USUARIO]:[CONTRASEÑA]@database-1.c702yeeu6qxd.us-east-2.rds.amazonaws.com:5432/[BASE_DE_DATOS]?schema=public"

# EJEMPLO (reemplaza con tus datos reales):
# DATABASE_URL="postgresql://ecomarket_user:mi_password_seguro@database-1.c702yeeu6qxd.us-east-2.rds.amazonaws.com:5432/ecomarket?schema=public"

# NOTAS:
# - El usuario por defecto suele ser "postgres" o el que configuraste
# - La base de datos por defecto suele ser "postgres" o el nombre que configuraste
# - Asegúrate de que no hay espacios en la URL